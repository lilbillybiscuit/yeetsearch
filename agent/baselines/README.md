# Harness Baselines

This directory is reserved for harness-owned baselines, reference oracles,
workload generators, and metric extraction helpers.

Implementation, experiment, verifier, falsifier, and replication agents should
treat this directory as read-only. Changes belong to the baseline agent and must
preserve correctness on the relevant property and oracle tests.
